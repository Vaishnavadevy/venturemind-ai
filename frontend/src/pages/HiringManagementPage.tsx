import { useMemo, useState } from 'react'
import { Link } from 'react-router-dom'
import { Button } from '@/components/common/Button'

export function HiringManagementPage() {
  const [title, setTitle] = useState('Customer Support Officer'); const [department, setDepartment] = useState('Operations'); const [experience, setExperience] = useState('1–2 years'); const [business, setBusiness] = useState('My business'); const [location, setLocation] = useState('Jaffna, Sri Lanka'); const [created, setCreated] = useState(false); const [copied, setCopied] = useState(false)
  const questions = [`Tell us about a time you solved a customer problem relevant to ${title}.`, `How would you prioritize competing tasks in ${department}?`, 'What information would you need before making a decision?', 'Describe how you would communicate a mistake to your manager.']
  const vacancy = useMemo(() => `${business} is hiring a ${title} in ${location}. Department: ${department}. Experience: ${experience}. We are looking for reliable, customer-focused applicants with clear communication and a willingness to learn. Send your CV and contact details to apply.`, [business, title, location, department, experience])
  const copyVacancy = async () => { try { await navigator.clipboard.writeText(vacancy); setCopied(true) } catch { setCopied(false) } }
  return <div className="mx-auto max-w-6xl space-y-7"><header><p className="text-sm font-bold uppercase tracking-widest text-brand-600">Hiring management</p><h1 className="mt-2 text-3xl font-bold">Build a structured hiring pack.</h1><p className="mt-2 text-slate-600">Generate a first draft, review it internally, then publish only the vacancy requirements—not interview questions or scorecards.</p></header><section className="grid gap-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-card md:grid-cols-3"><label className="text-sm font-semibold">Job title<input value={title} onChange={e=>setTitle(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 bg-white p-3 font-normal"/></label><label className="text-sm font-semibold">Department<input value={department} onChange={e=>setDepartment(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 bg-white p-3 font-normal"/></label><label className="text-sm font-semibold">Experience<input value={experience} onChange={e=>setExperience(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 bg-white p-3 font-normal"/></label><label className="text-sm font-semibold">Business name<input value={business} onChange={e=>setBusiness(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 bg-white p-3 font-normal"/></label><label className="text-sm font-semibold">Job location<input value={location} onChange={e=>setLocation(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 bg-white p-3 font-normal"/></label><div className="flex items-end"><Button className="w-full" onClick={()=>setCreated(true)}>Generate hiring pack</Button></div></section>{created && <><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-card"><h2 className="text-xl font-bold">{title} job description</h2><p className="mt-3 text-sm">Department: <strong>{department}</strong> · Experience: <strong>{experience}</strong> · Location: <strong>{location}</strong></p><h3 className="mt-5 font-bold">Responsibilities</h3><ul className="mt-2 list-disc space-y-2 pl-5 text-sm"><li>Deliver agreed work accurately and on time.</li><li>Communicate progress, customer needs, and blockers clearly.</li><li>Follow documented operational, privacy, and quality procedures.</li></ul><h3 className="mt-5 font-bold">Requirements</h3><p className="mt-2 text-sm">Relevant capability, reliable communication, willingness to learn, and evidence of responsible work behaviour.</p></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-card"><h2 className="text-xl font-bold">Interview and evaluation</h2>{questions.map((q,i)=><p key={q} className="mt-3 text-sm"><strong>0{i+1}.</strong> {q}</p>)}<h3 className="mt-5 font-bold">Scorecard</h3><p className="mt-2 text-sm">Rate communication, role capability, problem solving, reliability, and culture contribution from 1–5. Keep this internal—it is not part of the public job post.</p><h3 className="mt-5 font-bold">Offer checklist</h3><p className="mt-2 text-sm">Confirm salary, start date, probation, reporting line, working hours, benefits, and lawful contract terms before issuing an offer.</p></section></div><section className="rounded-3xl bg-slate-950 p-6 text-white shadow-card"><p className="text-sm font-bold uppercase tracking-widest text-violet-200">Vacancy publishing</p><h2 className="mt-2 text-2xl font-bold">Share this role with your community</h2><p className="mt-2 text-sm leading-6 text-slate-300">The editable post includes only the public job requirements. Direct publishing is a future OAuth integration; this version opens each platform for copy-and-paste posting.</p><textarea value={vacancy} readOnly rows={5} className="mt-5 w-full rounded-xl border border-white/15 bg-white/10 p-3 text-sm leading-6 text-white" /><div className="mt-4 flex flex-wrap gap-3"><Button onClick={() => void copyVacancy()}>{copied ? 'Vacancy copied' : 'Copy vacancy text'}</Button>{[['Facebook', 'https://www.facebook.com'], ['Instagram', 'https://www.instagram.com'], ['LinkedIn', 'https://www.linkedin.com'], ['WhatsApp Business', 'https://web.whatsapp.com']].map(([name, url]) => <a key={name} href={url} target="_blank" rel="noreferrer" className="rounded-xl border border-white/20 px-4 py-2.5 text-sm font-bold hover:bg-white/10">Open {name} ↗</a>)}<Link to="/posters" className="rounded-xl border border-white/20 px-4 py-2.5 text-sm font-bold hover:bg-white/10">Create hiring poster →</Link></div></section></>}</div>
}
