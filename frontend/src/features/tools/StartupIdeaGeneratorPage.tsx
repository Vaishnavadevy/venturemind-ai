import { useMemo, useState } from 'react'
import { Link } from 'react-router-dom'

import { Button } from '@/components/common/Button'

const modes = ['Practical', 'Balanced', 'Bold', 'Experimental'] as const
type Mode = (typeof modes)[number]
type Concept = { name: string; summary: string; revenue: string; risk: string; mvp: string }

function valueOr(value: string, fallback: string) { return value.trim() || fallback }

function createConcepts(industry: string, customer: string, skills: string, market: string, mode: Mode): Concept[] {
  const sector = valueOr(industry, 'local services')
  const audience = valueOr(customer, 'nearby customers')
  const capability = valueOr(skills, 'your existing skills')
  const place = valueOr(market, 'your chosen market')
  const direction = {
    Practical: ['Service Starter', 'a simple service fee or commission', 'A small local market may not pay enough at first.', 'Speak to 10 potential customers and deliver the service manually once.'],
    Balanced: ['Growth Hub', 'a monthly subscription with a paid starter package', 'Customer acquisition may cost more than expected.', 'Create one landing page and test one paid offer with 10 customers.'],
    Bold: ['Network Platform', 'a transaction fee plus premium business accounts', 'Building a two-sided network is difficult before demand is proven.', 'Recruit one customer group first and solve one repeatable workflow.'],
    Experimental: ['Insight Lab', 'pilot-project fees and research partnerships', 'The problem may be interesting but not urgent enough to buy.', 'Run five problem interviews before building any technology.'],
  }[mode]
  return [
    { name: `${sector} ${direction[0]}`, summary: `A focused ${sector.toLowerCase()} offer for ${audience.toLowerCase()} in ${place}, using ${capability.toLowerCase()} to solve one recurring problem.`, revenue: direction[1], risk: direction[2], mvp: direction[3] },
    { name: `${place} ${sector} Connect`, summary: `A curated discovery and coordination service helping ${audience.toLowerCase()} find and use better ${sector.toLowerCase()} options.`, revenue: 'listing fees, service packages, and optional subscriptions', risk: 'The offer needs clear differentiation from existing informal alternatives.', mvp: 'Build a simple directory or WhatsApp-based pilot with five providers.' },
    { name: `${sector} Insight Studio`, summary: `A lightweight advisory and data-supported service for ${audience.toLowerCase()} who need faster decisions in ${place}.`, revenue: 'one-time advisory packages with recurring reporting', risk: 'Customers may need education before they understand the value.', mvp: 'Offer a paid manual report to three early customers and record their feedback.' },
  ]
}

export function StartupIdeaGeneratorPage() {
  const [industry, setIndustry] = useState('')
  const [customer, setCustomer] = useState('')
  const [skills, setSkills] = useState('')
  const [market, setMarket] = useState('Sri Lanka')
  const [mode, setMode] = useState<Mode>('Balanced')
  const [submitted, setSubmitted] = useState(false)
  const concepts = useMemo(() => createConcepts(industry, customer, skills, market, mode), [industry, customer, skills, market, mode])

  return <div className="-mx-4 -my-8 min-h-[calc(100vh-6rem)] bg-[radial-gradient(circle_at_top_right,_rgba(196,181,253,0.42),_transparent_32rem)] px-4 py-5 sm:-mx-6 sm:px-6"><main className="mx-auto max-w-7xl space-y-6">
    <section className="rounded-[2rem] bg-slate-950 px-6 py-9 text-white shadow-float sm:px-10"><p className="text-sm font-bold uppercase tracking-[0.18em] text-violet-300">Free founder tool</p><h1 className="mt-3 max-w-3xl text-4xl font-bold tracking-tight sm:text-5xl">Find a startup direction that fits your real context.</h1><p className="mt-4 max-w-2xl text-lg leading-8 text-slate-300">Use your market knowledge, target customer, and strengths to create concept drafts. These are starting points for research—not validated businesses or promises of success.</p></section>

    <section className="grid gap-6 xl:grid-cols-[0.82fr_1.18fr]"><form className="rounded-3xl border border-slate-200 bg-white p-6 shadow-card dark:border-slate-800 dark:bg-slate-900" onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }}><div className="flex items-center justify-between"><div><p className="text-sm font-bold uppercase tracking-wider text-violet-700">Your inputs</p><h2 className="mt-1 text-2xl font-bold">Shape the opportunity</h2></div><span className="rounded-full bg-violet-100 px-3 py-1 text-xs font-bold text-violet-800">Step 1 of 2</span></div><p className="mt-3 text-sm leading-6 text-slate-600">You can edit every result. Nothing is saved automatically.</p><div className="mt-6 space-y-4"><label className="block text-sm font-semibold">Industry or business area<span className="text-rose-600"> *</span><input required value={industry} onChange={(event) => setIndustry(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent p-3 font-normal outline-none transition focus:border-violet-600 focus:ring-2 focus:ring-violet-100 dark:border-slate-700" placeholder="For example: food and beverage" /></label><label className="block text-sm font-semibold">Who do you want to help?<span className="text-rose-600"> *</span><input required value={customer} onChange={(event) => setCustomer(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent p-3 font-normal outline-none transition focus:border-violet-600 focus:ring-2 focus:ring-violet-100 dark:border-slate-700" placeholder="For example: university students" /></label><label className="block text-sm font-semibold">Your useful skills or access<input value={skills} onChange={(event) => setSkills(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent p-3 font-normal outline-none transition focus:border-violet-600 focus:ring-2 focus:ring-violet-100 dark:border-slate-700" placeholder="For example: cooking, design, supplier contacts" /></label><label className="block text-sm font-semibold">Market or location<input value={market} onChange={(event) => setMarket(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 bg-transparent p-3 font-normal outline-none transition focus:border-violet-600 focus:ring-2 focus:ring-violet-100 dark:border-slate-700" placeholder="For example: Jaffna, Sri Lanka" /></label></div><fieldset className="mt-6"><legend className="text-sm font-semibold">Choose an exploration style</legend><div className="mt-3 grid grid-cols-2 gap-2">{modes.map((item) => <button key={item} type="button" onClick={() => setMode(item)} className={`rounded-xl border px-3 py-3 text-sm font-bold transition ${mode === item ? 'border-violet-600 bg-violet-50 text-violet-800 ring-2 ring-violet-100 dark:bg-violet-500/10' : 'border-slate-200 text-slate-600 hover:border-violet-300 dark:border-slate-700 dark:text-slate-300'}`}>{item}</button>)}</div></fieldset><Button className="mt-7 w-full" type="submit">Generate concept drafts</Button></form>
      <div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-card dark:border-slate-800 dark:bg-slate-900"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm font-bold uppercase tracking-wider text-violet-700">Your concept drafts</p><h2 className="mt-1 text-2xl font-bold">{submitted ? 'Choose one direction to investigate' : 'Your ideas will appear here'}</h2></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600 dark:bg-slate-800 dark:text-slate-300">{mode} mode</span></div>{submitted ? <div className="mt-6 space-y-4">{concepts.map((concept, index) => <article key={concept.name} className="rounded-2xl border border-slate-200 p-5 transition hover:border-violet-300 dark:border-slate-700"><div className="flex gap-4"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-violet-100 text-sm font-extrabold text-violet-800">{index + 1}</span><div><h3 className="text-lg font-bold">{concept.name}</h3><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">{concept.summary}</p></div></div><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-3"><div className="rounded-xl bg-emerald-50 p-3 dark:bg-emerald-950/20"><dt className="font-bold text-emerald-800 dark:text-emerald-300">Revenue path</dt><dd className="mt-1 text-emerald-950 dark:text-emerald-100">{concept.revenue}</dd></div><div className="rounded-xl bg-amber-50 p-3 dark:bg-amber-950/20"><dt className="font-bold text-amber-800 dark:text-amber-300">Key risk</dt><dd className="mt-1 text-amber-950 dark:text-amber-100">{concept.risk}</dd></div><div className="rounded-xl bg-sky-50 p-3 dark:bg-sky-950/20"><dt className="font-bold text-sky-800 dark:text-sky-300">First MVP test</dt><dd className="mt-1 text-sky-950 dark:text-sky-100">{concept.mvp}</dd></div></dl></article>)}<div className="flex flex-wrap gap-3 border-t border-slate-200 pt-5 dark:border-slate-700"><Link to="/register"><Button>Start a full startup plan</Button></Link><Link className="rounded-lg border border-slate-300 px-5 py-3 text-sm font-bold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:text-slate-100" to="/learn">Learn how to test an idea</Link></div></div> : <div className="mt-6 grid min-h-96 place-items-center rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-8 text-center dark:border-slate-700 dark:bg-slate-950/30"><div><p className="text-lg font-bold">Start with two required details.</p><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-600 dark:text-slate-300">Enter an industry and customer group, then choose a style. VentureMind will create three editable directions with practical next tests.</p></div></div>}</div></section>
  </main></div>
}
