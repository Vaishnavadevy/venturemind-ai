# Frontend

React, TypeScript, Vite, Tailwind CSS, React Router, React Hook Form, Axios, and Recharts client.

## Run locally

```powershell
cd frontend
npm install
npm run dev
```

Copy `.env.example` to `.env` to customize the API URL. Routes are established now; their production screens are implemented in their corresponding modules.
